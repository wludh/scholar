= form_tag destroy_multiple_works_path, :method => :delete, :name => "works_form", :id => 'works_form' do
  #bulk_links
    = check_box_tag "select_works", "yes", false, :id => 'global-checkbox'
    %strong #{t('app.select_all')}
    |
    = submit_tag t('.submit')
    = hidden_field_tag "return_path", @return_path

  .col-xs-7= will_paginate @works

  %ol#work-list.col-xs-7.works.clear-both
    - @works.each do |work|
      = render "shared/proper_work", :work => work, :links => [:edit], :allow_select => true, :show_status => true

  .col-xs-7= will_paginate @works